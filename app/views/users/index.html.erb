<p><%= notice %></p>

<h1>ユーザー一覧</h1>

<div id="users">
    <% @users.each do |user| %>
        <%= render user %>
    <p>
        <%= link_to "詳細", user %>
    </p>
    <% end %>
</div>

<%= link_to "会員登録", new_user_path %>