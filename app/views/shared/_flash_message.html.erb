<% flash.each do |message_type, message| %>
  <% bootstrap_class = {
    "notice" => "success",
    "alert" => "danger"
}[message_type] || message_type %>
  <div class="alert alert-<%= bootstrap_class %>">
    <%= message %>
  </div>
<% end %>
