
<nav class="navbar navbar-expand-lg navbar-light fixed-top p-3">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown">
        <span class="navbar-toggler-icon"></span>
    </button>
    <%= link_to '栞灯 SHIORIBI', root_path, class: 'navbar-brand fs-2' %>

    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                <span class="nav-link"><%= current_user.name %></span>
            </li>
            <li class="nav-item d-flex align-items-center">
                <i class="bi bi-gear ml-2"></i>
                <%= link_to t('header.edit_user'),edit_user_path(current_user), class: 'nav-link' %></span>
            </li>
            <li class="nav-item d-flex align-items-center">
                <i class="bi bi-box-arrow-right ml-2"></i>
                <%= link_to t('header.logout'), logout_path, class: 'nav-link' %>
            </li>
        </ul>
    </div>
</nav>
