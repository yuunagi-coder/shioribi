<p><%= notice %></p>
<h2>遠くの灯火</h2>
<div id="posts">
<% if @posts.any? %>
    <% @posts.each do |post| %>
        <%= render partial: 'post', locals: { post: post, show_user_name: true } %>
    <% end %>
<% else %>
    <p>投稿がありません</p>
<% end %>
</div>

